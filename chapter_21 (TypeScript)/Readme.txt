// Install TypeScript
open terminal and use nodejs to install TypeScript
-- npm install -g typeScript

// convert TypeScript to Javascript
tsc sandbox.ts --outFile sandbox.js

if both .ts and .js are the same just run

tsc sandbox.ts -w